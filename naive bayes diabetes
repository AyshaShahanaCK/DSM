import pandas as pd
from sklearn.datasets import load_diabetes
from sklearn.model_selection import train_test_split
df-pd.read_csv('diabetes.csv')
x=df.iloc[:,0:8]
y=df.iloc[:,8]
print("target values \n",y)
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2, random_state=42) 
import numpy as np
from sklearn.metrics import accuracy_score
from sklearn.neighbors import KNeighborsClassifier
#if data set had @ values then use below commented #x_train = x_train.replace(0, x_train.mean()) #x_test = x_test.replace(0, x_test.mean())
knn=KNeighborsClassifier (n_neighbors=3) 
knn.fit(x_train, y_train)
j=knn.predict(x_test)
a = accuracy_score (y_test, j)
print(a)
new=[]
print("Enter 8 feature values for the new data point:")
for i in range(8):
    value= float(input(f"Feature {i+1}: ")) # Prompt for each feature
    new.append(value)
new = np.array(new).reshape(1, -1) #Reshape to match input shape print(new)
yn=knn.predict(new)
print(f'Prediction for new data point:',yn)
if yn==0:
    print('No diabetic')
else:
    print('diabetic')

